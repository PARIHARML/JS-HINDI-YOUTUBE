"use strict";// when we use  "use strict" in java script tab se hamara js code treat kiya jaayega as a newer version of java script//

// alert(3+3)//we are using node js not browser, it allowed in broeser only//

console.log(3 
    +3); // this is not best practise of writing code because code not looks nicely and readable we have follow best practice and code readibility should be high//

    console.log("Learning data types in js");

    let name = "Naveen"// string => ""
    let age =26        //number = range 2 to power 53 approx and for bigger num we use "bigint"//
    let isLoggedIn = false // true or false
      
    //null => standalone value in js
    //undefined => value not assined yet
    //symbol => its define uniqueness

    //object 

    console.log(name);
    console.log(age);
    console.log("Type of Age = ",typeof age);
    console.log("Type of Name = ",typeof name);
    console.log("Type of Null = ",typeof null); // type of null is object//
    console.log("Type of Undefined = ",typeof undefined);//typeof undefined iS itself undefined because ye apne aap ne ek type hai Java script mE now i understan//